#!/bin/bash
LAYOUT=$(/opt/homebrew/bin/aerospace list-windows --focused --format '%{window-layout}')

if [ "$LAYOUT" = "floating" ]; then
  open -g 'rectangle-pro://execute-action?name=left-half'
else
  /opt/homebrew/bin/aerospace move left
fi
